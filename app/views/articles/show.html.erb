<div class="container">
  <div class="row">
    <div class="col-md-8">
      <h1><%= @article.title %></h1> <small> <%= link_to 'Edit', edit_article_path(@article) %> | <%= link_to 'Destroy', @article, method: :delete, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %> </small>
      <p><%= @article.content %></p>
    </div>
  </div>
</div>
